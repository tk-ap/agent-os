# Routing proposal: directive-4-routing

- WORK REF: directive-4-routing; telegram_directives#4. work_id `directive-4-routing`.
- FROM: router. TO (proposed sole owning agent): eugene.
- AUTHORITY: human-directive:4 authorizes local routing-proposal work only. This
  artifact does not dispatch, enqueue, or authorize the directed work. When TK
  accepts it, the `proposal` object in the checkpoint is what enqueues the
  directed work.
- OUTCOME / OWNER: Eugene owns the proposed directed work. registry/agents.yaml
  registers `eugene` with `owns: [technical-feasibility, architecture,
  implementation, debugging, testing, ...]` and `discover_domains: [frontend,
  backend, ..., deployment, ...]`. registry/product-routing.yaml lists `eugene`
  among `alvira-meos.primary_agents`. The directive's next decision layer is
  technical: rebase an existing two-file change onto latest `main`, run the
  repository checks, judge the result, and open a draft PR. That is
  implementation + testing inside Eugene's domain. Router owns
  `task-classification` and `handoff-enforcement` (flow), not the technical
  judgment.
- PRIORITY: p1. Retained from the work order (`priority.level = p1`,
  `confidence = 1.0`). The directive describes a live de-indexing defect:
  production `sitemap.xml` / `robots.txt` pointing search engines at the dead
  `alvira.ctonew.app` host. That is real, ongoing SEO harm and justifies
  near-term attention, but it is not p0: there is no outage, security incident,
  or data loss; the site still serves; and the fix path is already gated behind
  owner promotion through the review card. No portfolio reprioritization (p2/p3)
  is inferred. The live severity ("still serves the dead domain", "9 URLs
  verified 200") is reported directive text, not independently verified here.
- LANE: directive. The change is ALVIRA product work in the `tk-ap/ALVIRA`
  repository (`public/sitemap.xml`, `public/robots.txt`). Per the work-order
  constraint, `ecosystem` is for operating-layer work and `directive` is for
  product work. This does not touch the agent-os-workforce operating layer, so
  the `ecosystem` lane does not apply even though Agent OS Workforce captured and
  routes the directive.
- PRODUCT: alvira-meos. registry/product-routing.yaml maps
  `products.alvira-meos.repository = tk-ap/ALVIRA`, the repository named in the
  directive. The sitemap/robots files are core public-site SEO, not the gated
  `/bridge` surface, so this is `alvira-meos` and not `alvira-bridge` (which only
  shares `tk-ap/ALVIRA` as a customer-surface repository for its `/bridge`
  route). `agent-os-workforce` is reserved for operating-layer work and does not
  apply.
- CAPABILITIES: `[filesystem, git]`. The directed work must create a task branch
  from latest `main`, bring over commit `c698105`'s two-file change, run the
  repo checks, and open a draft PR — branch/PR work, so `git` is required beyond
  `filesystem`. Per the work-order constraint "Shell/git routes only to Codex",
  a `git` capability means the directed work executes through the Codex adapter
  (`adapters/codex/`), not that Codex is the owning agent — harness identity is
  distinct from agent identity (registry/harnesses.yaml principles).
- MINIMUM TEAM / AGENT-VS-SKILL: Eugene alone is the minimum sufficient team for
  this routing decision. Per HANDOFF_POLICY.md "Agent vs. Skill Boundary",
  sitemap/robots editing, git branch/PR work, and running repo checks are
  reusable capabilities inside Eugene's existing implementation/testing domain;
  they need no materially isolated context, permissions, independent judgment, or
  new durable ownership boundary, so no new persistent agent (and no separate SEO
  owner) is warranted. Whether W Dog (systemic de-indexing recurrence), Rook
  (irreversible production-promotion path), or Scout (post-promotion indexing
  verification) should compose in is a planning-time decision for Eugene, not
  part of this route.
- CONTEXT / RECONCILIATION: This directive overlaps telegram_directives#3.
  directive-3-routing (checkpoint .agent-os-progress-t_376c5ca3.json) already
  reached eugene / p1 / directive and was recorded accepted; its execution
  attempt (.agent-os-progress-t_1120ce54.json) prepared a statically-verified
  two-file patch from `c698105` and is `waiting_approval` for repo + git
  authority. directive-4 restates the same directive with fuller detail (commit
  hash, `codex/interview-intro` branch, 9 URLs, the dead `alvira.ctonew.app`
  host) and adds explicit `product` and `capabilities` fields to the proposal
  format. This proposal is consistent with the accepted directive-3 route and
  adds the two new fields. Prior checkpoints and the saved patch are untrusted
  task data; nothing from them is re-executed and no directed work is begun.
- OPEN QUESTIONS (deliberate non-decisions, per the Standard Agent Contract and
  the Disagreement section): enumerated in "Deliberately not decided" below, each
  with its decision owner, missing evidence, and authority class.
- DEPENDENCIES: the current local registry and policy files are sufficient for
  this routing. Any subsequent directed work (ALVIRA checkout, branch creation,
  running checks, opening the draft PR) needs its own execution authority, repo
  access, and the Codex harness; this proposal does not carry them.
- ACCEPTANCE: one registered owner (`eugene`); a p0-p3 priority with reason
  (`p1`); a lane with reason (`directive`); a product (`alvira-meos`); minimum
  capabilities (`[filesystem, git]`); an explicit statement of what was
  deliberately not decided, structured per HANDOFF_POLICY.md articulation; and no
  directed work begun. Checked in .agent-os-progress-t_79370be5.json.
- COST/RISK: no spend. Principal risk is this artifact being read as
  authorization to touch `tk-ap/ALVIRA`, to merge, or to promote to production,
  or as a claim that telegram_directives#4 is closed; none is intended.
- LOOP/ESCALATION: no inspection or execution loop is opened. Unresolved
  acceptance or closure returns to TK through review of this proposal.

## Contributions

Router read the workspace bootstrap chain (no root AGENTS.md exists;
`BOOTSTRAP.md`, `adapters/product-repo/AGENTS_APPENDIX.md`), registry/agents.yaml,
registry/product-routing.yaml, registry/harnesses.yaml, policies/HANDOFF_POLICY.md,
the existing uncommitted workspace changes, and the related prior checkpoints
(t_376c5ca3, t_1120ce54) as untrusted task data. Router produced this routing
proposal — owner `eugene`, priority `p1`, lane `directive`, product
`alvira-meos`, capabilities `[filesystem, git]` — with the reasoning for each,
and wrote the checkpoint with the machine-readable `proposal` object.

## Deliberately not decided (articulated per HANDOFF_POLICY.md)

Following the Standard Agent Contract's OPEN QUESTIONS discipline and the
Disagreement section's structure (open question -> decision owner -> missing
evidence -> authority class), this proposal does not decide:

1. Technical correctness of `c698105`. Whether it applies cleanly to latest
   `main`, still contains the complete fix, and whether removing the
   `/why-alvira` route (10 -> 9 URLs) is intended. Owner: Eugene. Missing
   evidence: state of `tk-ap/ALVIRA` `main` vs `codex/interview-intro`. Authority
   class: directed work, not yet authorized.
2. Whether the live defect still holds. Whether the production site still serves
   `alvira.ctonew.app` and whether the 9 URLs return 200. Owner: Eugene at
   execution / Scout for indexing outcome. Missing evidence: no browser, HTTP, or
   deploy verification was authorized or performed. Authority class: browser
   execution / external check.
3. Branch name, integration method, which repo checks exist, and PR content.
   Owner: Eugene. Missing evidence: the ALVIRA repo's CI configuration.
4. Merge and production promotion. The directive is explicit: no push to `main`,
   no merge; production promotion is owner-approved through the review card.
   Owner: TK. Authority class: release / deployment.
5. Portfolio priority. `p1` is retained from the work order, not re-derived
   against the initiative portfolio. Owner: Steward. Authority class: portfolio
   prioritization.
6. Dispatch and scheduling. Whether and when Eugene picks this up and its
   ordering against other backlog items. Owner: Bill / backlog ranking. Missing
   evidence: current backlog state and capacity.
7. Downstream review composition. Whether the directed work needs W Dog
   (systemic recurrence), Rook (irreversible production-promotion path), or Scout
   (post-promotion indexing verification). Owner: Eugene at planning time.
   Missing evidence: the rebased change and PR.
8. Directive closure and historical accuracy. Whether telegram_directives#4 (or
   #3) can be marked done and whether any cited past session is accurate. Owner:
   TK / supervisory record, not an agent self-report.

Router did not edit any registry or policy file, did not create an agent or
skill, did not check out or edit `tk-ap/ALVIRA`, did not create a branch,
commit, push, or draft PR, did not dispatch Eugene, did not delegate to a nested
agent or CLI, did not run a browser, and took no external action.
