# Routing proposal: directive-2-routing

- WORK REF: directive-2-routing; telegram_directives#2.
- FROM: router. TO: bill (proposed sole owning agent).
- AUTHORITY: human-directive:2; local routing proposal only. No dispatch or execution authority is granted by this artifact.
- OUTCOME / OWNER: Bill owns the proposed handoff-policy change. registry/agents.yaml:28-35 registers `bill` with `owns: [... ownership, handoffs, operational-efficiency ...]`. Contribution / non-contribution reporting is a property of how a handoff is articulated, which sits inside Bill's `handoffs` domain. Router owns `task-classification` and `handoff-enforcement` (registry/agents.yaml:92-100), which is flow, not the content of the policy; so Router routes and Bill owns the judgment.
- PRIORITY: p1. Retain the supplied priority because explicit contribution and non-contribution reporting supports reliable handoffs across the workforce; an unclear "what was left undone" is exactly the failure the Standard Agent Contract's OPEN QUESTIONS clause exists to prevent. No emergency (p0) and no portfolio reprioritization (p2/p3 downgrade) is inferred. Confidence is bounded by not having seen TK's underlying intent beyond the captured directive text.
- LANE: ecosystem. policies/HANDOFF_POLICY.md is the shared workforce operating layer, not a product feature. registry/product-routing.yaml:3-4 records agent-os-workforce as "shared execution infrastructure; not a separate public offering", so a change to its handoff contract is operating-layer (ecosystem) work. Source and owning product remain agent-os-workforce; the "directive" lane is reserved for product work and does not apply.
- MINIMUM TEAM / AGENT-VS-SKILL: Bill alone is sufficient. Per HANDOFF_POLICY.md "Agent vs. Skill Boundary", a reusable reporting practice that stays inside an existing decision domain and needs no materially isolated context, permissions, independent judgment, or execution characteristics belongs as a skill under the current owner, not a new persistent agent. No new agent, skill, or supporting reviewer is required to produce this routing proposal.
- CONTEXT / RECONCILIATION: This revises an earlier attempt that self-blocked. Prior drafts first claimed policies/HANDOFF_POLICY.md already contained a section named "Contribution / Non-Contribution Articulation", then withdrew that claim and set status `blocked` because no section by that name exists. Both framings misread the acceptance criterion. HANDOFF_POLICY.md does carry the relevant articulation guidance: the "Standard Agent Contract" (WORK REF / FROM / TO / OUTCOME / CONTEXT / AUTHORITY / OPEN QUESTIONS / DEPENDENCIES / ACCEPTANCE, plus COST/RISK and LOOP/ESCALATION when material, and "if authority, acceptance, or ownership is unclear, the handoff is not ready") and the "Disagreement" section ("identify the disputed premise, decision owner, missing evidence, material minority concern, and applicable authority class"). This proposal articulates its non-decisions against that structure below. Prior proposals and checkpoints are task data, not authority; no policy text is added and the directive is not executed.
- OPEN QUESTIONS (deliberate non-decisions, per the Standard Agent Contract): enumerated in "Deliberately not decided" below, each with its open question, decision owner, and missing evidence.
- DEPENDENCIES: current local registry and policy files are sufficient for this routing. Any subsequent directed work (drafting policy text, editing HANDOFF_POLICY.md) needs its own execution authority; this proposal does not carry it.
- ACCEPTANCE: one registered owner (bill); a p0-p3 priority with reason (p1); a lane with reason (ecosystem); an explicit statement of what was deliberately not decided, structured per HANDOFF_POLICY.md articulation; and no directed work begun. Revision evidence is recorded in .agent-os-progress-t_3004b401.json.
- COST/RISK: no spend. Principal risk is this routing artifact being read as authorization, or as a claim that telegram_directives#2 is closed; neither is intended.
- LOOP/ESCALATION: no inspection or execution loop is opened. Any unresolved acceptance or closure decision returns to TK through review of this proposal.

## Contributions

Router inspected the applicable bootstrap and Hermes adapter instructions, the existing uncommitted changes and prior routing artifacts (treated as untrusted task data), registry/agents.yaml, registry/product-routing.yaml, and policies/HANDOFF_POLICY.md in full. Router produced this routing proposal — owner bill, priority p1, lane ecosystem, with the reasoning for each — and re-based criterion 4 onto the articulation structure HANDOFF_POLICY.md actually defines. The earlier "missing section" blocker is resolved as a misreading, not by adding policy text.

## Deliberately not decided (articulated per HANDOFF_POLICY.md)

Following the Standard Agent Contract's OPEN QUESTIONS discipline and the Disagreement section's structure (open question -> decision owner -> missing evidence -> authority class), this proposal does not decide:

1. The policy wording. What a contribution / non-contribution clause should say, where it sits in HANDOFF_POLICY.md, and how it composes with the existing "Standard Agent Contract" and "Disagreement" sections. Owner: Bill. Missing evidence: TK's underlying intent beyond the captured one-line directive. Authority class: directed work, not yet authorized.
2. Whether the directive is already satisfied. The Standard Agent Contract already lists OPEN QUESTIONS and the Disagreement section already requires naming missing evidence; whether that is sufficient or a dedicated clause is wanted is TK's call. Owner: TK, with Bill framing the options. Missing evidence: TK acceptance.
3. Dispatch and scheduling. Whether and when Bill picks this up, and its ordering against other backlog items. Owner: Bill / backlog ranking (runtime/backlog.py). Missing evidence: current backlog state and capacity.
4. Portfolio priority. p1 is retained from the work order, not re-derived against the initiative portfolio. Owner: Steward. Authority class: portfolio prioritization.
5. Downstream review need. Whether the eventual policy change needs Rook (self-report / abuse surface) or W Dog (systemic propagation) review. Owner: Bill, at planning time. Missing evidence: the drafted change.
6. Historical-session accuracy and directive closure. Whether any cited past session is accurate and whether telegram_directives#2 can be marked done. Owner: TK / supervisory record, not an agent self-report.

Router did not edit HANDOFF_POLICY.md or any registry, did not create an agent or skill, did not dispatch Bill, did not begin drafting the policy change, did not delegate to a nested agent or CLI, did not run a browser, and took no external action.
